{
  "name": "sondaclick",
  "version": "1.0.0",
  "description": "Aplicación SondaClickMX",
  "main": "build_installer.js",
  "scripts": {
    "build:backend": "cd backend && pyinstaller --onefile --name SondaClickBackend --noconsole app.py", 
    "build:frontend:package": "cd frontend && npm run package", 
    "build:frontend:msi": "cd frontend && npm run build-msi",
    "build:all": "npm run build:backend && npm run build:frontend:package && npm run build:frontend:msi"
  },
  "devDependencies": {
    "electron-builder": "^24.13.3",
    "electron-wix-msi": "^5.1.3",
    "fs-extra": "^11.3.0"
  }
}
